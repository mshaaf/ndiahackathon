.DS_Store
.env
.env.*
!.env.example
*.pem
*.key
.venv/
__pycache__/
.pytest_cache/
.coverage
htmlcov/
node_modules/
dist/
.worktrees/
.claude-flow/
.swarm/
.claude/

# Run artifacts and unreviewed downloads; reviewed synthetic fixtures belong in fixtures/synthetic/.
data/
artifacts/

# datasets/ IS committed so the team can clone and run without re-downloading.
# The sponsor package is the exception: it stays local until its handling terms
# are recorded in RIGHTS.md. Put it here and it will not be committed by accident.
datasets/sponsor/
*.sqlite
*.sqlite3
*.db
